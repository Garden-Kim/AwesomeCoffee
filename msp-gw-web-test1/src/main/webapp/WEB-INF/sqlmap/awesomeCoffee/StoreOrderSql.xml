<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="StoreOrder">

	<!-- 발주 insert -->
	<insert id="insertStoreOrder" parameterType="map">
		INSERT INTO(STOREORDER_NUM, STORE_NUM, FOOD_NUM, 
					STOREORDER_QTY, STOREORDER_DATE, STOREORDER_PRICE)
		VALUES(#{sotreOrderNum}, #{storeNum}, #{foodNum}, #{sotreOrderQty},
			   sysdate, #{storeOrderPrice})
	</insert>
</mapper>