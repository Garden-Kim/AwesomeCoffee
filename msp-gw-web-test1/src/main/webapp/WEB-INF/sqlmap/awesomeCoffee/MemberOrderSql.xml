<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Order">

	<insert id="insertOrder">
		INSERT INTO MEMBER_ORDER (order_num, order_price, order_time, takeout, member_num, cook_state)
		VALUES
	    	(order_seq.nextval, #{orderPrice},
	    	TO_CHAR(SYSDATE, 'RRRR-MM-DD HH24:MI:SS'),
	    	#{takeout}, #{memberNum}, #{cookState})
	</insert>

	<delete id="deleteOrder">
		delete
		from member_order
		where order_num = #{orderNum}
	</delete>

	<select id="selectAllMemOrder"
		resultType="awesomeCoffee.dto.MemberOrderDTO">
		select *
		from member_order
		where member_num = #{memberNum}
		order by order_num
	</select>
	
	<select id="selectAllEmpOrder"
		resultType="awesomeCoffee.dto.MemberOrderDTO">
		select *
		from member_order
		where cook_state = 'N'
		order by order_num
	</select>
	
	<update id="updateOrderCookState">
		update member_order
		set cook_state = #{cookState}
		where order_num = #{orderNum}
	</update>
	
	<update id="updateOrderTakeout">
		update member_order
		set takeout = #{takeout}
		where order_num = #{orderNum}
	</update>
	
	<update id="updateOrderPrice">
		update member_order
		set order_price = #{orderPrice}
		where order_num = #{orderNum}
	</update>

	
	
	
	
	
	
	
	
	
	
	
	
	
</mapper>